{
  "name": "brt-inventaris-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adapter layer for BRT Inventaris — ports (repository interfaces) + the parse boundary between Google Sheets and the pure domain.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "fake-indexeddb": "^6.2.5",
    "typescript": "~5.9.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}

# Copy to `app/.env` and fill in before building. Without it the app still works — it simply asks
# every device for the gateway address, which is what this exists to remove.
#
# Apps Script -> Deploy -> Manage deployments -> the URL ending in /exec
VITE_GATEWAY_URL=
